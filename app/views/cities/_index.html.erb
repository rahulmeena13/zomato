<% if @cities.length > 0 %>
    <% @cities.each do |city| %>
        <%=link_to city.name, list_restaurants_path(@country.name,city.name) %>
        <br />
    <% end %>
<% else %>
    No Cities Listed
<% end %>