<% if @countries.length > 0 %>
    <% @countries.each do |country| %>
        <%=link_to country.name, list_cities_path(country.name) %>
        <br />
    <% end %>
<% else %>
    No Countries in Database, please seed the database using rake db:seed
<% end %>